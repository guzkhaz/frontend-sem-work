export const includesIgnoredCase = (firstVal: string, secondVal: string) => firstVal.toLowerCase().includes(secondVal.toLowerCase())


export const equalsIgnoredCase = (firstVal: string, secondVal: string) => firstVal.toLowerCase() === secondVal.toLowerCase()